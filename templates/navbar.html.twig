<nav class="navbar navbar-expand-lg navbar-white text-dark bg-white mb-5 ml-2">
        <a class="navbar-brand" href="#"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
        data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" 
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto" id="textColorNav">
                <li class="nav-item active">
                    <a class="nav-link" aria-current="page" href="{{ path("home") }}" 
                    id="navbarHome">Accueil</a>
                </li>
                <li class="nav-item dropdown" id="navbarHome">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" 
                    role="button" data-bs-toggle="dropdown" aria-haspopup="true" 
                    aria-expanded="false">
                        Événementiel
                    </a>
                    <ul class="dropdown-menu ml-1" aria-labelledby="dropdownMenuButton1">
                        {% for navHomeBlock in home.getHome %}
                            <li class="dropown-item">
                                <a href="
                                {{ path('page_show', {slug:navHomeBlock.page.slug}) }}" 
                                id="lienNav"
                                >{{ navHomeBlock.title | raw }}
                                </a>
                            </li>
                        {% endfor %}
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("partenaires") }}" tabindex="-1" 
                    id="navbarHome" >Partenaires</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("galerie") }}" tabindex="-1" 
                    id="navbarHome" >Galerie</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('recettes_list') }}" 
                    tabindex="-1" id="navbarHome">Recettes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("contact") }}" id="navbarHome">Demander un devis</a>
                </li>
            </ul>
        </div>
    </div>
</nav>