<header class="container-fluid"> 
    <div class="row text-center">
        <div class="col">
            <nav class="navbar navbar-expand-lg">
                    <button class="navbar-toggler mx-left" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0" id="textColorNav">
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page" href="{{ path("app_home") }}" id="navbarHome">Accueil</a>
                            </li>
                            <li class="nav-item dropdown" id="navbarHome">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    Événementiel
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                                    {% for navHomeBlock in home.getHome %}
                                    <li><a href="{{ path('page_show', {slug:navHomeBlock.page.slug}) }}" id="lienS5Home">{{ navHomeBlock.title | raw }}</a></li>
                                    {% endfor %}
                                </ul>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path("app_partenaires") }}" tabindex="-1" id="navbarHome" >Partenaires</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('receipts_list') }}" tabindex="-1" id="navbarHome">Recettes</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path("app_contact") }}" id="navbarHome">Demander un devis</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        </div>
</header>

